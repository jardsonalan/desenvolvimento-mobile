import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    margin: 14,
    marginBottom: 2,
    borderWidth: 1,
    borderColor: '#727000',
    borderRadius: 8,
    backgroundColor: '#feffc5'
  },
  pending: {
    fontSize: 14,
    fontWeight: 500,
    color: '#727000'
  }
})