import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    marginBottom: 16,
    backgroundColor: '#00722c',
  },
  title: {
    fontSize: 14,
    fontWeight: 600,
    color: '#fff'
  },
  semester: {
    fontWeight: 400
  }
})